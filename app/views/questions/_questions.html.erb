<%= hidden_field_tag :direction, params[:direction] %>
<%= hidden_field_tag :sort, params[:sort] %>
<div class="table-responsive">
  <table class="table table-bordered table-condensed">
      <tr>
        <th class="tb-hidden"> <%= "id" %></th>
        <th> <%= "ФИО" %></th>
        <th class="tb-hidden"> <%= "Возраст клиента" %></th>
        <th> <%= "Категория" %></th>
        <th class="tb-hidden"> <%= "Город клиента" %></th>
        <th> <%= "Дата создания" %></th>
      </tr>

      <% @questions.each do |question| %>
        <tr>
          <td class="tb-hidden"><%= link_to question.id, question_path(question) %></td>
          <td><%= link_to question.last_name << "  " << question.first_name << " " << question.patronymic_name, question_path(question) %> </td>
          <td class="tb-hidden"><%= question.age %></td>
          <td><%= question.category %></td>
          <td class="tb-hidden"><%= question.city %></td>
          <td><%= question.created_at %></td>
        </tr>
      <% end %>
  </table>
</div>
